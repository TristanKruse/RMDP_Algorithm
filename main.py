import logging
logging.basicConfig(level=logging.DEBUG, format='%(asctime)s - %(levelname)s - %(message)s [Line: %(lineno)d]')

# run Environment
# Run RL-Algorithm

# Hyperparameters in einem seperaten File einstellen


if __name__ == '__main__':
